<ContentDialog
    x:Class="TVClient.WolContentDialog"
    x:Uid="WolContentDlg"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TVClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Error"
    PrimaryButtonText="WakeOnLan"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Uid="WolContentDialogTextBlock" Text="Connection not possible." />
        <Rectangle Grid.Row="1" Height="2" Fill="White" HorizontalAlignment="Stretch" />
        <TextBlock Grid.Row="2" x:Name="detailsTextBlock" TextWrapping="Wrap" />

    </Grid>
</ContentDialog>
